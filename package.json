{
  "private": true,
  "name": "gh-resolve-ref",
  "version": "1.0.0",
  "description": "A Cloudflare Worker that resolves a reference (a branch or tag name) of a GitHub repository to the SHA-1 of the corresponding commit.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {},
  "dependencies": {}
}
